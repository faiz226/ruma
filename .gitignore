# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — NEVER commit these
.env
.env.*
server/.env
*.env

# Database dumps & backups — NEVER commit these (may contain guest PII)
*.backup
*.dump
*.sql.gz
*.pg
*_backup
*.bak
# Note: supabase/migrations/*.sql is intentionally NOT ignored (those are schema-only migration files)

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
